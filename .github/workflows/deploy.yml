name: Deploy to Vercel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Skip - Using Vercel Git Integration
        run: |
          echo "This project uses Vercel's native Git integration for deployments."
          echo "Vercel automatically deploys on push to main."
          echo "This workflow is disabled to prevent conflicts with Vercel's built-in CI/CD."